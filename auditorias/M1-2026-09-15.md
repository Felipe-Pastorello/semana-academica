## Matriz de rastreabilidade

| Regra | Origem | Teste que comprova | Veredito |
|---|---|---|---|
| R1 | P-10 | api/verificacoes/fatia1.spec.js:16 «recusa GET /salas sem cabeçalho X-Usuario» | PROVA FRACA |
| R2 | — | — | SEM ORIGEM, SEM PROVA |
| R3 | — | — | SEM ORIGEM, SEM PROVA |
| R4 | — | — | SEM ORIGEM, SEM PROVA |
| R5 | P-01 | — | SEM PROVA |
| R6 | P-02 | — | SEM PROVA |
| R7 | P-03 | — | SEM PROVA |
| R8 | P-04 | — | SEM PROVA |
| R9 | P-05 | — | SEM PROVA |
| R10 | P-06 | — | SEM PROVA |
| R11 | P-08 | — | SEM PROVA |
| R12 | P-07 | — | SEM PROVA |
| R13 | P-09 | — | SEM PROVA |
| R14 | — | — | SEM ORIGEM, SEM PROVA |
| R15 | P-10 | — | SEM PROVA |

## Suíte

`npm test` →
```
> semana-academica-api@1.0.0 test
> node --test verificacoes/fatia1.spec.js

TAP version 13
# Subtest: Fatia 1 — Consulta de Salas e Atividades
    # Subtest: recusa GET /salas sem cabeçalho X-Usuario
    ok 1 - recusa GET /salas sem cabeçalho X-Usuario
      ---
      duration_ms: 165.5741
      ...
    # Subtest: retorna 200 OK e a lista de salas em GET /salas com usuario valido
    ok 2 - retorna 200 OK e a lista de salas em GET /salas com usuario valido
      ---
      duration_ms: 16.038
      ...
    # Subtest: retorna 200 OK e array de atividades em GET /atividades com usuario valido
    ok 3 - retorna 200 OK e array de atividades em GET /atividades com usuario valido
      ---
      duration_ms: 5.7532
      ...
    1..3
ok 1 - Fatia 1 — Consulta de Salas e Atividades
  ---
  duration_ms: 204.3542
  type: 'suite'
  ...
1..1
# tests 3
# suites 1
# pass 3
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 494.8268
```

## Achados

1. [PROVA FRACA] R1 — A spec exige em `specs/M1-inscricoes.md:76` que o cabeçalho `X-Usuario` contenha um ID de usuário válido e retorne `401 USUARIO_DESCONHECIDO` caso ausente ou desconhecido. O teste em `api/verificacoes/fatia1.spec.js:16` verifica apenas a ausência do cabeçalho `X-Usuario`, mas não valida a rejeição quando um usuário inválido/desconhecido é enviado. Cenário que expõe: Requisição com `X-Usuario: usuario-invalido` para `GET /salas`.
Commit: 1cd473c

2. [SEM ORIGEM] R1, R2, R3, R4 e R14 — As regras R1, R2, R3, R4 e R14 não possuem perguntas dedicadas na entrevista de requisitos (`entrevistas/M1-inscricoes.md`). R1, R2, R3 e R4 são citadas na ordem de precedência de erros em P-10 (`entrevistas/M1-inscricoes.md:94`), enquanto R14 não possui nenhuma menção na entrevista.
Não procede: R1, R2, R3 e R4 derivam de requisitos transversais de infraestrutura e autorização descritos no contrato global da API (`contrato-api.md`). R14 descreve os parâmetros de busca padrão da rota e deve ser mantida como especificação do contrato de listagem.

3. [SEM PROVA] R2 — A spec exige em `specs/M1-inscricoes.md:77` que operações de escrita/cancelamento por participantes retornem `403 SOMENTE_ORGANIZACAO`. Nenhum teste executa este cenário. Cenário que expõe: Requisição `POST /atividades` enviada por participante `p-carla`.
Commit: pendente (aguardando implementação da Fatia 2)

4. [SEM PROVA] R3 — A spec exige em `specs/M1-inscricoes.md:78` que rotas com `:id` retorne `404 NAO_ENCONTRADO` se o ID não existir. Nenhum teste executa este cenário. Cenário que expõe: `GET /atividades/atv_inexistente`.
Commit: pendente (aguardando implementação da Fatia 3)

5. [SEM PROVA] R4 — A spec exige em `specs/M1-inscricoes.md:79` que corpo JSON inválido ou com campos faltantes/inválidos retorne `422 DADOS_INVALIDOS`. Nenhum teste executa este cenário. Cenário que expõe: `POST /atividades` com JSON malformado.
Commit: pendente (aguardando implementação da Fatia 2)

6. [SEM PROVA] R5 — A spec exige em `specs/M1-inscricoes.md:80` que palestras tenham exatamente 1 encontro e minicursos de 2 a 5 encontros, retornando `422 QUANTIDADE_DE_ENCONTROS`. Nenhum teste executa este cenário. Cenário que expõe: `POST /atividades` de minicurso com 6 encontros.
Commit: pendente (aguardando implementação da Fatia 2)

7. [SEM PROVA] R6 — A spec exige em `specs/M1-inscricoes.md:81` a validação de encontros (duração entre 1h e 4h, datas de 19 a 23/10/2026, restrição de horário entre 23:00 e 00:30, sem sobreposição), retornando `422 ENCONTRO_INVALIDO`. Nenhum teste executa este cenário. Cenário que expõe: Encontro com duração de 30 minutos ou término às 23:30.
Commit: pendente (aguardando implementação da Fatia 2)

8. [SEM PROVA] R7 — A spec exige em `specs/M1-inscricoes.md:82` que `vagas > 0` e `vagas <= capacidade` da sala, retornando `422 VAGAS_ACIMA_DA_CAPACIDADE`. Nenhum teste executa este cenário. Cenário que expõe: `POST /atividades` com 50 vagas em sala de capacidade 40.
Commit: pendente (aguardando implementação da Fatia 2)

9. [SEM PROVA] R8 — A spec exige em `specs/M1-inscricoes.md:83` que haja intervalo mínimo de 15 minutos entre encontros na mesma sala, retornando `409 CONFLITO_DE_SALA`. Nenhum teste executa este cenário. Cenário que expõe: `POST /atividades` com encontro iniciado 10 minutos após o fim de outra atividade na mesma sala.
Commit: pendente (aguardando implementação da Fatia 2)

10. [SEM PROVA] R9 — A spec exige em `specs/M1-inscricoes.md:84` a imutabilidade dos campos `id`, `tipo`, `salaId` e `encontros` em `PATCH`, retornando `422 CAMPO_NAO_EDITAVEL`. Nenhum teste executa este cenário. Cenário que expõe: `PATCH /atividades/:id` tentando alterar `salaId`.
Commit: pendente (aguardando implementação da Fatia 4)

11. [SEM PROVA] R10 — A spec exige em `specs/M1-inscricoes.md:85` que o novo número de vagas em `PATCH` seja `>= ocupadas`, retornando `409 VAGAS_ABAIXO_DOS_INSCRITOS`. Nenhum teste executa este cenário. Cenário que expõe: `PATCH /atividades/:id` reduzindo vagas abaixo de `ocupadas`.
Commit: pendente (aguardando implementação da Fatia 4)

12. [SEM PROVA] R11 — A spec exige em `specs/M1-inscricoes.md:86` que `PATCH` em atividade cancelada retorne `422 ATIVIDADE_CANCELADA`. Nenhum teste executa este cenário. Cenário que expõe: `PATCH /atividades/:id` em atividade cancelada.
Commit: pendente (aguardando implementação da Fatia 4)

13. [SEM PROVA] R12 — A spec exige em `specs/M1-inscricoes.md:87` que cancelamento após o início retorne `422 ATIVIDADE_JA_INICIADA` e em atividade já cancelada retorne `422 ATIVIDADE_CANCELADA`. Nenhum teste executa este cenário. Cenário que expõe: `POST /atividades/:id/cancelamento` quando `agora >= inicio` do 1º encontro.
Commit: pendente (aguardando implementação da Fatia 4)

14. [SEM PROVA] R13 — A spec exige em `specs/M1-inscricoes.md:88` que o campo `situacao` seja derivado de `agora` (`"prevista"`, `"em_andamento"`, `"encerrada"`, `"cancelada"`). Nenhum teste executa este cenário. Cenário que expõe: `GET /atividades/:id` durante o horário de execução de um encontro.
Commit: pendente (aguardando implementação da Fatia 3)

15. [SEM PROVA] R14 — A spec exige em `specs/M1-inscricoes.md:89` a filtragem por `?dia=` e `?tipo=` em `GET /atividades`. O teste em `api/verificacoes/fatia1.spec.js:37` verifica apenas a listagem geral sem filtros. Cenário que expõe: `GET /atividades?dia=2026-10-19&tipo=minicurso`.
Commit: pendente (aguardando implementação da Fatia 3)

16. [SEM PROVA] R15 — A spec exige em `specs/M1-inscricoes.md:90` a validação de precedência estrita de erros em requisições com múltiplos problemas. Nenhum teste executa este cenário. Cenário que expõe: `POST /atividades` enviado por participante com 0 encontros, testando se retorna `403 SOMENTE_ORGANIZACAO` antes de `422 QUANTIDADE_DE_ENCONTROS`.
Commit: pendente (aguardando implementação da Fatia 4)

## Veredito

O módulo M1 não pode ser aceito no estado atual: apenas a Fatia 1 foi parcialmente implementada e comprovada, estando faltantes as implementações e suítes de testes das Fatias 2, 3 e 4 para comprovar as regras R2 a R15 e complementar a prova da regra R1.
